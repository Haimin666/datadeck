# DataDeck 后续开发计划

本文件只记录当前代码基线之后的计划。DataAgent 已完成基础可用版本；由于业务数据、指标口径和表结构仍较零散，DataAgent 的质量增强须在业务资料完成整理后按阶段推进。

## 通用 Agent

### P0：稳定性、工具错误与权限边界

- [x] Run 状态、排队、取消、SSE 断线恢复和服务重启恢复
- [x] 连续消息按线程串行执行，避免请求互相覆盖
- [x] 工具异常转换为 Agent 可理解的工具错误
- [x] 文件工作目录白名单和 Skill/MCP/定时任务授权
- [x] 高风险工具审批与运行审计

### P1：任务规划、上下文与 MCP 稳定性

- [x] Todo 任务清单和执行状态展示
- [x] 长对话摘要、记忆和上下文预算控制
- [x] 最大执行步数和单次运行超时
- [x] MCP 工具发现超时、缓存失效和失败降级
- [x] 运行状态恢复与工具清单统一展示

### P2：多 Agent 协作

- [x] 主 Agent 启动、查询、等待和取消 Subagent
- [x] Subagent 独立 Thread/Run 落库并回传结果
- [x] 子 Agent 白名单和单层递归限制
- [x] 子任务事件可查询，复用现有 Run/SSE 事件模型
- [x] 多 Subagent 并行编排、依赖关系和主 Agent 汇总
- [x] 跨进程子任务事件汇总与统一结果校验

### P3：复杂工作流（后续重点）

复杂工作流是“规划、执行、审批、恢复、汇总”组成的可追踪任务流程，不等同于一次 Subagent 调用。建议在 P2 基础上分阶段建设。

#### W0：工作流边界与统一协议

- [ ] 定义工作流状态：`draft/running/paused/waiting_approval/completed/failed/cancelled`
- [ ] 定义节点类型：Agent、Subagent、工具、人工审批、条件分支、汇总
- [ ] 统一输入、输出、错误、重试和超时协议
- [ ] 建立 workflow/run/node 三层标识与父子关系

验收：任意一次工作流都能查询当前状态、所在节点、历史事件和最终结果。

#### W1：工作流计划与执行引擎

- [ ] 将 Todo 计划转换为可执行节点图
- [ ] 支持串行节点和无依赖节点并行执行
- [ ] 支持节点依赖、前置条件和最大并发数
- [ ] 支持节点超时、重试次数和失败终止策略
- [ ] 支持保存执行快照，服务重启后恢复

验收：一个包含 3 个串行节点和 2 个并行节点的任务可以稳定完成并正确汇总。

#### W2：条件分支与人工介入

- [ ] 支持基于工具结果的条件分支
- [ ] 支持人工审批、暂停和继续
- [ ] 审批拒绝时按策略终止、跳过或回退节点
- [ ] 支持用户修改后续任务参数并从指定节点继续

验收：高风险节点审批拒绝不会误执行，恢复后不会重复执行已完成节点。

#### W3：结果汇总与质量校验

- [ ] 主 Agent 汇总多个 Subagent 结果
- [ ] 标记成功、失败、跳过和超时节点
- [ ] 汇总前校验结果完整性和来源
- [ ] 关键结果支持规则校验或二次 Agent 复核
- [ ] 向用户展示执行摘要、产物、错误和待办项

验收：部分子任务失败时，最终结果不会把失败结果伪装成成功结论。

#### W4：工作流产品化

- [ ] 新增工作流运行详情页和节点时间线
- [ ] 支持从历史运行复制并重新执行
- [ ] 支持工作流模板保存、编辑、启停和删除
- [ ] 支持定时任务触发工作流
- [ ] 支持工作流产物写入个人空间

验收：用户可以创建、运行、暂停、恢复、查看和重跑一个工作流。

#### W5：生产治理

- [ ] 多实例节点抢占锁和幂等执行
- [ ] 跨实例事件同步与断线重连
- [ ] 工作流资源、Token、耗时和失败率统计
- [ ] 节点级权限、审计和敏感信息脱敏
- [ ] 压力测试、故障注入和数据恢复演练

验收：单实例故障、网络断开和重复请求不会造成重复执行或永久卡住。

### 复杂工作流首个示例

“读取项目文件 → 分析数据 → 并行生成报告和图表 → 人工确认 → 写入个人空间 → 创建定时任务”。

首个版本只支持固定节点协议和有限分支，不建设任意拖拽式编排器；待运行稳定后再考虑可视化设计器。

## DataAgent

### D1：业务资料整理（前置条件）

目标：把三类来源分别放到最适合的系统中，避免把 OMD 的库表管理信息复制成一份过时的 RAG 文档。

#### D1.0：范围、权威性与安全边界

- [ ] 以 Wiki 页面 `70331927` 为业务文档入口，确认子页面、版本和附件范围
- [ ] 确认 OMD 的目标服务、数据库和 schema 范围，以及可查询的血缘深度
- [ ] 扫描 `../lion_dw`，建立 Python/SQL 文件、任务、输入表、输出表和分区字段清单
- [x] 确认目标格式为 `../ossie` 的 Apache Ossie 语义模型规范（当前仓库版本 `0.2.0.dev0` 草案）
- [ ] 明确来源冲突规则：指标口径以生效版本的 Wiki 为准，库表和血缘以 OMD 为准，数仓代码用于补充 ELT 实现证据
- [ ] 标记敏感字段、可查询范围、数据责任人和可用于 RAG 的内容边界

验收：形成来源清单、权限清单和权威性规则；任何资料都能回答“来自哪里、何时生效、谁负责”。

#### D1.1：Wiki 业务资料物料

- [ ] 提取页面正文、标题层级、页面 ID、版本、更新时间、父子关系和附件引用
- [ ] 识别业务术语、指标定义、计算公式、适用范围、例外规则和别名
- [ ] 按“一个主题/一个指标/一个规则”整理成可引用的规范片段
- [ ] 为每个片段保留 `source_type=wiki`、`page_id`、标题路径、版本和原文定位
- [ ] 业务文档进入知识库和 RAG；不把未经整理的整页 HTML 直接作为最终知识

验收：抽样业务问题能够检索到唯一或明确的生效口径，并返回页面和段落依据。

#### D1.2：OMD 库表、血缘和 ELT 边物料

- [ ] 通过 OMD 工具获取 database/schema/table、表描述、负责人、标签、字段和上下游血缘
- [ ] 将血缘转换为标准 ELT 边：`source -> transform/job -> target`，保留任务名、字段映射、方向和来源定位
- [ ] 区分 OMD 直接观测信息与从 SQL 代码推断的信息，不混淆置信度
- [ ] 库表管理和实时血缘以 OMD 工具为查询入口；RAG 只保存必要的解释性索引或引用，不替代 OMD
- [ ] 对无血缘、循环血缘、失效表和代码无法解析的边建立待核验清单

验收：给定表或字段可以通过 OMD 查询上下游，并能追溯到对应 ETL 任务或代码文件；推断边均有标记。

#### D1.3：本地数仓代码物料

- [ ] 只读扫描 `../lion_dw` 下 Python/SQL，记录文件路径、Git commit/hash、任务名和更新时间
- [ ] 解析 `INSERT/CREATE TABLE AS/INSERT OVERWRITE`、来源表、目标表、JOIN、过滤条件、分区字段和指标表达式
- [ ] 优先使用 SQL AST 解析，解析失败时保留原文件片段并进入人工复核清单
- [ ] 生成代码证据索引：任务 → 输入表 → 转换逻辑 → 输出表，并与 OMD 血缘做差异比对
- [ ] 代码解释材料可进入 RAG；库表存在性和线上管理状态仍以 OMD 返回为准

验收：随机抽取任务能展示完整输入、转换、输出链路，并明确“代码观测”和“OMD 观测”的差异。

#### D1.4：统一物料模型和去重

- [ ] 建立来源 manifest、规范文档、指标对象、ELT 边对象和代码证据对象
- [ ] 所有对象包含稳定 ID、来源、版本/hash、更新时间、责任人、权限标签和状态
- [ ] 采用内容 hash 和来源 ID 幂等入库，支持更新、删除和失效标记
- [ ] 建立 Wiki 指标、OMD 表字段、代码表达式之间的交叉引用

建议的内部指标规范字段：`metric_id`、`name`、`aliases`、`definition`、`formula`、`grain`、`time_window`、`filters`、`dimensions`、`numerator`、`denominator`、`source_tables`、`source_fields`、`owner`、`version`、`effective_at`、`status`、`evidence`。

验收：同一指标或同一代码物料重复同步不会产生重复记录，且旧版本仍可追溯。

### D2：检索质量

#### D2.1：落入 RAG 的内容

- [ ] Wiki 业务规则、术语、指标解释和代码转换说明进入知识库
- [ ] 指标规范对象生成可读文档，并保留规范格式原文/导出文件和 Wiki 证据
- [ ] OMD 仅按需生成带 TTL 的引用索引；查询库表、字段和血缘时优先调用 OMD 工具
- [ ] 不将完整 OMD 快照当作长期静态知识，以免与线上元数据不一致

#### D2.2：索引与回答

- [ ] 基于标题、主题域、指标、表名和代码结构进行基础切分
- [ ] 混合检索、重排序、低置信度拒答和来源引用
- [ ] 支持按来源、主题域、版本、权限和时间过滤
- [ ] 处理物料更新、删除、失效和重建，保证 BM25/Qdrant 与数据库状态一致

验收：回答业务口径引用 Wiki/指标物料；回答库表血缘引用 OMD；回答转换逻辑同时给出代码证据，三者不能互相冒充。

### D3：数据权限与 Text2SQL

- [ ] 表级、字段级和行级权限与 OMD/项目权限联动
- [ ] 敏感字段脱敏、禁止表清单、查询成本和扫描范围限制
- [ ] Text2SQL 先通过 OMD 校验表字段和血缘，再生成 SQL
- [ ] SQL 结果校验、空结果解释、时间范围提示和异常结果复核
- [ ] 指标查询强制绑定指标规范，禁止仅凭相似字段名猜口径

验收：无权限表不可生成或执行 SQL；指标、字段和来源均可解释；查询失败不会伪造结果。

### D4：业务评测与分析表达

#### D4.1：评测集

- [ ] 建立 Wiki 口径问答、指标计算、表字段、上下游血缘、ELT 逻辑和取数问题集
- [ ] 覆盖同义词、冲突口径、无权限、无血缘、代码解析失败和空结果场景
- [ ] 为每题保存期望来源、关键字段、SQL 约束和可接受答案范围

#### D4.2：指标与数据质量

- [ ] 量化 RAG 召回率、引用正确率、口径准确率、OMD 工具成功率和 SQL 成功率
- [ ] 检查“观测事实/代码推断/模型推测”三类结论是否正确标注
- [ ] 完善数据时间范围、过滤条件、依据、结果解释和图表输出

验收：发布前评测达到约定阈值；低置信度问题能够明确告知缺资料，而不是给出看似确定的答案。

### DataAgent 物料建设分阶段计划

#### M0：目标确认与盘点

- [ ] 确认 OSSIE 格式、OMD 服务范围、Wiki 子树范围和权限边界
- [ ] 产出 `data_material_manifest`，列出来源、对象数量、版本、责任人和待核验项

#### M1：抽取与规范化

- [ ] 完成 Wiki 业务资料抽取和指标候选识别
- [ ] 完成 OMD 库表/字段/血缘/ELT 边抽取
- [ ] 完成 `../lion_dw` Python/SQL 扫描和代码证据索引
- [ ] 产出冲突、解析失败和缺失元数据清单

#### M2：规范对象与存储

- [ ] 落地统一物料模型、版本、hash、权限和来源引用
- [ ] 将已确认指标导出为目标 OSSIE 格式；格式未确认前禁止伪造兼容文件
- [ ] 完成物料幂等更新、删除/失效和审计记录

#### M3：RAG 和 OMD 联动

- [ ] Wiki/指标/代码解释物料进入 RAG，完成切分、索引和引用
- [ ] DataAgent 按意图路由：业务口径走 RAG，库表/血缘走 OMD，取数先 OMD 后 SQL
- [ ] 增加 OMD 与代码血缘交叉核验和冲突提示

#### M4：评测、刷新和上线

- [ ] 完成黄金问题集、权限测试和失败场景测试
- [ ] 增加定时刷新、增量同步、失败重试、变更审计和物料质量报告
- [ ] 达到验收阈值后再扩大 Wiki、OMD 和代码扫描范围

每一阶段必须先完成“产物可检查、来源可追溯、失败可重跑、权限可验证”四项验收，未通过不得进入下一阶段。

## 共同基础设施

- [ ] 多实例 Agent Run 锁和跨实例事件同步
- [ ] 统一工具调用审计、成本统计和告警
- [ ] 生产环境压力测试与故障演练
