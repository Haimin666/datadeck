# DataDeck 统一分层与 Agent 优化 TODO

> 本文件是当前阶段的唯一执行清单。以实际代码、接口和测试为准，不以旧文档中的完成标记为准。
>
> 创建时间：2026-09-14

## 总原则

1. **只有一个运行时装配入口**：手动会话、恢复、审批继续、定时任务和 Subagent 全部进入 `AgentRuntimeAssembler`，再创建本次运行的 Graph。
2. **不做兼容方案**：不保留旧路由、旧响应字段、旧存储、旧装配入口或双写逻辑。迁移只允许作为一次性数据迁移，完成后删除旧代码和旧表。
3. **核心层纯净**：`src/datadeck` 不得导入 `server`，不得读取数据库、环境宿主服务或平台全局状态；所有平台能力必须通过 ports/context 注入。
4. **一份事实来源**：运行时资源、工具、RAG 物料、指标和任务状态分别只有一个权威模型；索引和缓存只能重建，不能成为事实来源。
5. **阶段闸门**：当前阶段的代码、数据迁移、回归测试、删除清理和验收未完成，不进入下一阶段，不对外宣称完成。
6. **新增功能同规**：后续新增 Skill、MCP、工具包、Agent、知识库和工作流必须接入统一契约与装配器，不得新增旁路。

## 当前基线

- 阶段 1～7 的代码、迁移和针对性回归测试已完成（附件解析旁路在 `20260914_0020`、部门权限旁路在 `20260914_0021` 一次性移除）；阶段 8 的部署代码已完成，干净服务器
  smoke/fault 演练仍需在目标服务器执行。
- `src/datadeck` 不导入 `server`；正式 AgentRun、恢复、审批继续、排队、定时任务和
  Subagent 都通过 `AgentRuntimeAssembler` 生成请求级快照。
- 工具配置只保存能力包；静态包和动态 `package:mcp:<server_slug>` 均通过同一
  `ToolDescriptor`/运行时白名单/Trace 契约处理。
- `KnowledgeBase/KnowledgeDocument/KnowledgeChunk` 是 RAG 事实模型；PostgreSQL 是
  文档与关键词检索事实来源，Qdrant 只是可重建向量索引。
- 当前本地测试环境允许通过 `DATADECK_AGENT_ALLOW_ALL_ACTIONS` 跳过审批；生产环境的配置
  校验会拒绝该开关，发布前必须使用审批模式完成一次回归。

## 当前增量收敛（2026-09-15）

- [x] 修复无 `workspace` 模块权限的临时会话创建路径：不再访问空项目对象，并补充角色已分配 Agent 的回归测试。
- [x] 明确“角色模块权限”和“Agent 运行能力”分离：用户只需被分配 Agent，即可使用该 Agent 已装配能力；前端模块权限不再错误地限制 Agent 内部工具。
- [x] 通用 Agent 与 DataAgent 提示词分流；通用 Agent 不再因挂载数据工具而自动注入 DataAgent 工作流。
- [x] Agent 身份默认为空，能力说明按运行时实际工具和 Skill 动态生成。
- [x] Agent 详情返回当前 Agent 的运行资源目录，保证知识库、Skill、MCP 与 `@` 候选一致；移除全局过期资源追加逻辑。
- [x] Agent 运行资源遵守用户隔离：仅加入当前用户可访问资源和当前 Agent 明确配置的资源，不把其他用户私有知识库/Skill 全量暴露给运行时。
- [x] 子 Agent 运行能力与 Agent 管理模块解耦；已授权 Agent 可使用其编排的子 Agent，但普通用户仍不因此获得 Agent 管理页面权限。
- [x] 未注册的历史内置 Skill 从管理、运行和 `@` 候选中过滤，不删除用户原始文件。
- [x] 无 workspace 模块权限的会话不创建隐式项目并强制保存空 `project_id`。
- [x] LangGraph checkpoint 外部表不存在时，历史迁移跳过外部表修复，由 `AsyncPostgresSaver.setup()`负责新库初始化。
- [x] 缺少 `source_type` 的历史普通文档按上传文档参与 RAG chunk 修复。
- [x] 清理前端已移除的部门管理残留：用户分页筛选、用户状态、个人设置展示和共享用户标签不再读写部门字段。
- [x] Docker 管理命令在 Compose 校验前预检 `.env.docker`/`.env` 的 dotenv 语法，并报告异常行号，避免把终端输出误当配置。
- [x] Docker 首次 `init` 自动生成随机 JWT 与 PostgreSQL 密码；已有 `.env.docker` 不自动覆盖，避免改变现有数据库凭据；非法 dotenv 行会清理临时校验文件并报告行号。
- [x] 运行时诊断区分“默认策略正常跳过”和“资源不可用/拒绝”，不再为每个未挂载资源重复推送“默认策略未挂载”。
- [x] 历史消息重建保留 Run/Event 的原始时间戳，不再把刷新后的消息统一改成当前时间。
- [x] 历史 Run 在创建时间相同时按稳定的 `run_id` 排序，避免事件交错导致消息归并错误。
- [x] 清理通用 Agent 的过时 Text2SQL 描述；通用 Agent 与 DataAgent 的能力边界在内置后端元数据和初始化描述中保持一致。
- [x] DataAgent 仅在代码逻辑类问题中按需挂载 `code_search`；普通口径、结构和取数问题不再暴露代码检索工具。
- [x] 闲聊分类要求消息仅包含问候语，避免“你好，帮我……”被误判为闲聊；纯闲聊运行不装配宿主工具。
- [x] DataAgent 代码类任务在统一装配阶段追加 `code_search`，使 Runtime Snapshot 与实际工具列表保持一致。
- [x] 纯闲聊在核心工具解析入口再次隔离，避免工作区工具绕过宿主门控进入正式 Graph。
- [x] 公共平台能力统一收敛为固定的 `package:platform`；配置页不再显示为可移除、运行时也不再使用“固定单工具 + 可选能力包”的双重契约。
- [x] 补齐 OMD、SQL、指标和基础工具的服务端模块权限映射，并以工具注册表完整性测试阻止新增工具绕过二次权限校验。
- [x] OMD Service 工具支持六类真实类型：Hive、Doris、Mysql、Oracle、Looker、CustomDashboard；返回 `service_category` 与 `service_type`，数据库查询与看板查询分流。

## 阶段 1：核心层边界彻底收敛

状态：`DONE`（2026-09-14）

目标：让 `src/datadeck` 成为可独立测试的 Agent 内核，完全不依赖 `server`。

任务：

- [x] 扫描 `src/datadeck` 全部 import、运行时回调和默认全局状态，建立边界清单。
- [x] 将模型供应商缓存、平台模型信息等宿主能力定义为 port，由 `server` 注入实现。
- [x] 删除 `platform_model_provider.py` 对 `server.services` 的直接导入。
- [x] 删除核心构造函数中的宿主兼容回调；模型、工具、Skill、MCP、工作区和用户追问都从统一 Runtime Context 获取。
- [x] 生产代码禁止直接调用未装配的 `get_graph()`；只允许元信息场景使用显式的无用户运行时建图接口。
- [x] 增加架构测试：导入 `datadeck` 不得加载 `server`，核心测试不得依赖 PostgreSQL、Redis、Qdrant。
- [x] 删除确认无调用方的旧装配函数、旧 hook 和旧资源解析代码。

验收：`rg` 扫描无 `src/datadeck -> server` 依赖；所有正式入口只经过装配器；核心包可脱离 FastAPI 宿主独立完成单元测试。

## 阶段 2：统一运行时装配协议

状态：`DONE`（2026-09-14）

目标：所有 Agent 运行入口使用同一个不可变资源快照和生命周期。

任务：

- [x] 固化 `RuntimeResourceSnapshot` 的版本、用户、Agent、Thread、Project、权限、工具包、知识库、Skill、MCP、工作区、定时和 Subagent 字段。
- [x] 明确资源选择语义：`None` 是默认策略，空列表是明确不挂载，非法资源直接返回结构化错误。
- [x] 将手动运行、SSE 恢复、审批继续、排队、定时任务、Subagent 和重试全部改为调用同一个 `assemble → run` 流程。
- [x] 装配器统一负责资源查询、授权、实例化、失败诊断和工具白名单；业务路由不得自行拼装工具。
- [x] 运行期间只消费本次快照，禁止使用跨用户共享的可变授权对象。
- [x] 删除 `agents_provider` 中绕过装配器的构造和兼容 hook 传递。
- [x] 为资源缺失、禁用、越权、加载超时和实例化失败定义不同错误码及 Trace 事件。

验收：对同一个 Agent，五类入口生成相同规则的资源快照；跨用户、跨线程和运行中配置更新不会串资源；任何旁路装配调用在测试中失败。

## 阶段 3：工具包与统一工具 Schema

状态：`DONE`（2026-09-14）

目标：工具包是唯一挂载单位，管理页、运行时和审计使用同一份工具描述。

任务：

- [x] 让 `ToolDescriptor` 真正贯穿工具注册器、工具包、API、运行时白名单、审批和 Trace。
- [x] 统一字段：`slug`、`package_slug`、名称、描述、分类、来源、版本、风险级别、参数 Schema、可见性和启用状态。
- [x] 保留一个权威工具列表接口和一种响应格式，删除 `value` 等历史别名及重复 options 接口。
- [x] 工具包内部成员默认只作为运行时能力，不作为 Agent 编辑器的独立配置项；前端只选择工具包。
- [x] 内置 DataAgent 能力包固定挂载且不可移除；通用 Agent 仅看到明确授权和配置的包。
- [x] Skill 依赖工具、MCP 工具、工作区工具、定时任务和 Subagent 工具全部由包展开器生成。
- [x] 统一工具冲突、超时、审批、取消、失败和审计事件。
- [x] 删除旧工具注册表、重复分类字段和仅为旧前端保留的适配响应。

验收：前端展示的工具包与实际注册能力一致；未挂载/未授权工具无法调用；每次工具调用都能追溯到包、Agent、用户和 `run_id`。

## 阶段 4：DataAgent 确定性数据工作流

状态：`DONE`（2026-09-14）

目标：DataAgent 保留 Agent 自主判断，但数据工具必须服从可验证的工作流状态，禁止无用调用和越级执行。

任务：

- [x] 定义统一数据意图：闲聊、业务口径、指标分析、表结构、血缘、表同步、SQL 取数和无法识别。
- [x] 定义数据工作流状态：意图、证据、OMD 服务、表结构、SQL 校验、执行、结果校验和回答来源。
- [x] 业务口径/指标问题先检索 OSSIE 指标与业务 RAG；库表/字段/血缘优先调用 OMD；取数先 OMD 后 SQL。
- [x] 表同步问题直接进入 DBA 能力包，生成同步 SQL 和 JSON；不得让通用 Agent 自动获得数据工具。
- [x] OMD 未给出 service 时先搜索全部可用 service，命中多个时通过 `ask_user_question` 明确询问，不猜测。
- [x] SQL 只允许单条只读查询，并强制完成表结构确认、语法校验、危险操作校验和结果校验。
- [x] 工具前置条件由服务端状态机校验，不仅依赖 Prompt 或关键词。
- [x] 每个数据结论输出来源类型：RAG、OSSIE、OMD、代码证据或实时查询；缺证据必须拒答或追问。
- [x] 增加闲聊、指标、表同步、OMD 多命中、无表、无权限、空结果和工具失败回归测试。

验收：通用 Agent 不会因数据问题误调用数据工具；DataAgent 的表同步可稳定生成 SQL/JSON；每次运行能还原完整工具链和失败原因。

## 阶段 5：知识库、RAG 物料和索引统一

状态：`DONE`（2026-09-14）

目标：知识库模块是 RAG 物料唯一管理入口，数据库是唯一事实来源，Qdrant/BM25 只是可重建索引。

任务：

- [x] 以 `KnowledgeBase/KnowledgeDocument/KnowledgeChunk` 作为唯一 RAG 物料模型。
- [x] 一次性把 `AgentMaterial` 的有效内容、来源、层级、Hash 和版本迁移到正式知识库字段/元数据。
- [x] 删除 `AgentMaterial` 表、独立 materials API、旧 `/api/rag/documents` 和进程内文档事实存储。
- [x] 知识库模块保留“知识库/物料/代码仓库”多 Tab，但全部读取统一知识库模型；不再出现第二套 RAG 管理入口。
- [x] Wiki、业务文档、OSSIE 指标和代码解释进入 RAG；OMD 的库表状态和实时血缘仍通过 OMD 工具查询，不保存为长期事实快照。
- [x] 统一文档切分、确定性 chunk ID、增量更新、删除、失效、重建和版本追踪。
- [x] Qdrant 与 PostgreSQL 使用同一文档版本；索引失败要落状态并允许重试，不能改变事实数据。
- [x] RAG 结果必须带知识库、文档、chunk、来源、版本和分数；按权限和来源过滤。
- [x] 增加重启恢复、重复导入、更新、删除、向量失败和跨知识库隔离测试。

验收：只通过知识库模块即可管理和预览所有 RAG 物料；重启后索引可从数据库恢复；删除文档后所有索引和检索结果同步消失。

## 阶段 6：权限、数据安全和 Text2SQL 收敛

状态：`CODE_DONE`（2026-09-14；生产数据源策略需按部署环境配置）

目标：权限由服务端和数据源策略决定，任何前端参数都不能扩大访问范围。

- [x] 发布前关闭 `DATADECK_AGENT_ALLOW_ALL_ACTIONS`，恢复高风险工具人工审批并完成权限回归测试。

任务：

- [x] 明确模块、知识库、Agent、Skill、MCP、项目、工作区、任务和数据源的授权关系。
- [x] 知识库采用明确的 `private/shared/public` 范围和 ACL；共享知识库对有模块权限的用户可见，不依赖 uid 猜测。
- [x] OMD 查询允许配置范围内的全部 service；SQL 数据访问由部署数据源账号和只读 SQL 安全策略负责。
- [x] 落地可配置的 schema、表级和字段级禁止边界；行级权限由目标数仓授权模型负责，不能由通用 SQL 代理臆造。
- [x] 查询前执行只读、扫描范围、最大行数和成本限制；字段脱敏/行级策略由目标数仓账号负责。
- [x] 所有路径参数、collection、Agent、项目和文件路径在服务端重新解析并校验归属。
- [x] 日志和 Trace 脱敏，不记录 Token、Key、密码、完整文件内容和隐私数据。
- [x] 增加跨用户、共享知识库、越权 Agent、恶意 SQL 和路径穿越回归测试。

验收：无权用户无法检索、生成或执行对应数据；指标和 SQL 的字段来源可解释；所有安全拒绝都有明确错误和审计记录。

## 阶段 7：持久化、运行状态和可观测性统一

状态：`DONE`（2026-09-14）

目标：页面状态、任务状态和 Agent 状态都由持久化运行事件驱动，断线、刷新和重启不再出现永久加载。

任务：

- [x] 统一 `workflow/run/event/tool_call/approval` 的 ID、父子关系、状态枚举和时间字段。
- [x] 所有状态迁移由服务端定义，禁止前端自行推断“运行中/成功/失败”。
- [x] SSE 只作为事件传输；断线后按 `run_id + cursor` 补发事件，最终状态从数据库读取。
- [x] 工具执行、审批等待、ask-user、超时、取消、重试和恢复全部持久化。
- [x] 重启恢复只恢复可恢复状态，已完成节点不可重复执行；异常必须进入 failed 并保存原因。
- [x] 缓存设置上限、TTL、失效和重建机制；缓存不得保存跨用户授权状态。
- [x] Trace 在对话页面显示摘要，详细工具链、快照和错误可展开查看。
- [x] 增加连续消息、快速切换会话、SSE 中断、浏览器刷新、进程重启、重复请求和部分失败测试。

验收：任何运行都能通过 `run_id` 查询当前状态、工具链、资源快照和失败原因；前端不会无依据显示永久“加载中”。

## 阶段 8：部署、清理和发布验收

状态：`CODE_DONE`（2026-09-14；待干净服务器 smoke）

目标：干净服务器可用一套命令部署当前代码，升级不丢数据，发布不带历史死代码和敏感配置。

任务：

- [x] Docker 构建继续使用国内镜像源，并优化依赖缓存层；源码变更只重建必要层。
- [x] `docker-manage.sh` 统一负责初始化、迁移、物料注入、启动、健康检查、日志、停止和升级。
- [x] 明确并挂载 PostgreSQL、Qdrant、Redis、应用数据、上传文件和知识库物料目录；禁止用删除 volume 解决问题。
- [x] 部署启动只执行 Alembic migration，不在应用启动阶段增加 DDL。
- [x] 删除旧路由、旧模型、旧 API、旧前端 view、旧静态资源和无调用方代码；删除前完成一次性数据迁移和校验。
- [x] 提供可重复执行的 `docker-manage.sh smoke`：检查四个容器健康和登录、Agent、工具包、知识库、指标、定时任务核心 API。
- [x] 提供可重复执行的 `docker-manage.sh tool-probe`：在应用容器网络内逐项验证统一能力包、正式 Runtime 装配、SQL、OMD、RAG、指标、Skill、工作区、定时任务和已配置 MCP，不调用模型额度。
- [x] 提供需显式确认的 `docker-manage.sh fault-drill`：演练 PostgreSQL 未就绪、Qdrant 不可用、Redis 重启、应用重启和可选 SSE 断流，不删除数据卷。
- [ ] 在干净服务器执行完整 smoke：迁移、登录、Agent 对话、Skill、MCP、知识库导入/RAG、指标、OMD、定时任务和重启恢复。
- [ ] 在目标服务器执行故障演练，并单独验证模型超时、SSE 断开后的最终状态和恢复。
- [x] 本地执行后端 compile/test、前端 unit/build/lint、迁移 head 检查和 `git diff --check`。
- [x] 已从 Git 索引移除 `.env.docker`，本地部署文件仍保留。
- [ ] 发布方轮换历史暴露的密钥，并在部署环境更新对应 Token/Key。
- [x] Docker 管理命令启动前校验生产 JWT、全权限开关和 PostgreSQL 密码占位值，配置检查不再输出密钥。
- [x] README、RULES 和部署说明只保留新架构，不再描述旧方案或兼容入口。

验收：干净服务器可以一键启动；所有核心依赖 healthy；数据、索引、Agent 运行和用户上传内容重启后仍可用；旧入口和旧代码已删除。

## 执行顺序与交付规则

严格按以下顺序执行：

```text
1 核心边界
  ↓
2 运行时装配
  ↓
3 工具 Schema
  ↓
4 DataAgent 工作流
  ↓
5 知识库/RAG
  ↓
6 权限与数据安全
  ↓
7 持久化与可观测性
  ↓
8 部署与发布
```

每个阶段交付必须包含：

- 代码和一次性数据迁移；
- 旧路径删除结果；
- 对应回归测试和验收记录；
- 更新后的架构文档；
- 下一阶段可以直接依赖的稳定契约。

在阶段验收完成前，不进入下一阶段，也不把“已有部分实现”标记为完成。

## 阶段 9：知识物料分层与公共物料空间

状态：`COMPLETED`

进展：阶段9实现、真实数据迁移、幂等复跑、代码索引、公共物料访问和回归验收已完成。

目标：将“面向业务问答的知识”与“原始导入物料”彻底分离。知识库只管理业务文档和审核后的指标解释；原始 Wiki、Git 代码、导入快照和人工上传源文件通过个人空间的只读公共路径查看，不再把 JSON 快照和原始代码当作普通知识文档展示。

### 9.1 物料分类和事实来源

- [x] 固化四类权威数据：业务文档、OSSIE 指标、数仓代码、OMD 实时元数据。
- [x] 业务文档唯一事实来源为 `KnowledgeDocument` 的业务类文档；原始 `wiki_extract` 已归档。
- [x] OSSIE 正式口径唯一事实来源为 `MetricRegistry`；RAG 不覆盖正式定义。
- [x] 数仓代码以 Git 快照为事实来源，使用 `CodeRepositoryFile` 保存 commit/path 索引。
- [x] OMD 的 Service、Schema、表结构、字段、血缘和产出状态以实时 OMD 工具为事实来源。
- [x] 知识文档、指标和代码索引统一返回 source kind、source id、版本、路径和证据字段。

### 9.2 清理与一次性迁移

- [x] `migrate_knowledge_layers.py` 支持盘点报告，原始文档不删除。
- [x] Wiki/代码证据已生成业务 Markdown 草稿和 OSSIE 候选。
- [x] 代码物料已移出默认 RAG，并建立按 commit 的文件索引。
- [x] OSSIE 按 `ossie_name` 幂等合并，冲突保留证据并进入审核态。
- [x] 已清理 14,896 个派生 chunk 和对应 Qdrant 向量。
- [x] 迁移脚本已真实执行并重复执行验证为 0 修改。

### 9.3 个人空间公共物料路径

- [x] 新增 `/public/knowledge-materials` 只读入口和“公共知识物料”前端入口。
- [x] 服务端按知识库、来源类型、仓库/页面和路径生成虚拟文件树。
- [x] 写入、删除、建目录和上传接口均拒绝公共路径。
- [x] 文档与代码节点返回来源、版本、hash、仓库、分支和 commit 元数据。
- [x] 文本支持查看/下载；二进制和超大文件只返回元数据。
- [x] 登录用户按 knowledge 或 workspace 权限访问，公共物料不按创建者隔离。
- [x] 已完成路径穿越、符号链接逃逸和凭证暴露边界检查。
- [x] Git 拉取只更新快照、commit 和代码索引，不再改动普通业务知识文档。
- [x] 前端已将公共物料与个人工作区并列展示并标记只读。

### 9.4 代码解析和按需检索

- [x] Git 拉取与快照索引、语义产物生成已分离。
- [x] 按 `repository_id + branch + path + commit` 幂等保留历史版本。
- [x] 代码业务文档和指标候选默认标记 draft/needs_review。
- [x] `code_search` 支持仓库、commit、路径、表名、字段名和关键词。
- [x] 返回片段、文件、行号、commit、关联表和来源仓库，不注入完整代码。
- [x] SQL AST 已提取表、字段、Join、过滤、聚合、分区；失败保留状态。
- [x] 代码结果可生成业务文档草稿和 MetricRegistry 审核候选。
- [x] DataAgent 仅在代码逻辑验证场景挂载 `code_search`。

### 9.5 Agent 和检索链路

- [x] `rag_search` 只检索业务文档，不检索原始 OMD/代码快照。
- [x] `metric_lookup` 返回正式指标及审核状态并提示冲突。
- [x] `omd_*` 按需查询实时元数据。
- [x] `code_search` 作为 DataAgent 独立能力包挂载。
- [x] 检索结果携带来源、版本、路径、证据和运行 Trace 关联信息。

### 9.6 验收闸门

- [x] 知识库页面隐藏原始代码、摘要 JSON 和 OMD 快照 JSON。
- [x] 公共知识物料路径可查看授权知识库的原始文档、Wiki、Git 代码和导入物料。
- [x] 公共路径只读且不能越权读取私有工作区或凭证文件。
- [x] 业务问题走业务 RAG，结构/血缘问题走 OMD，代码逻辑问题走 `code_search`。
- [x] 代码更新按 commit 增量建立索引，旧版本可追溯且不污染默认 RAG。
- [x] Alembic、PostgreSQL、Qdrant、代码索引和迁移脚本已验证重启/重复执行一致性。
- [x] 跨用户权限、迁移幂等、公共路径安全、代码检索、RAG 隔离和工具装配回归通过。

## 阶段验收记录

- 阶段 1：`src/datadeck` 边界扫描和核心架构测试通过。
- 阶段 2：运行快照、统一装配、资源诊断和正式运行入口测试通过。
- 阶段 3：工具包、动态 MCP 工具包 `package:mcp:<slug>`、统一工具描述和 Trace 测试通过；旧 `context.mcps` 已由 `20260914_0019` 一次性迁移并删除。
- 阶段 4：DataAgent 意图门控、RAG/OMD/SQL 前置条件、同步任务和追问路径测试通过。
- 阶段 5：知识库事实模型、持久化 chunk、Qdrant 缺失索引按需重建、物料管理和前端统一入口测试通过；启动恢复事务已提交，避免 chunk 修复回滚。
- 阶段 6：模块/资源授权、只读 SQL、schema/表/字段禁止边界、脱敏日志和附件/制品路径边界检查已实现；部门权限范围已在 `20260914_0021` 一次性收敛为全局/指定用户；行级权限必须由目标数仓账号/策略提供。
- 阶段 6 补充：角色模块权限已进入请求级 Runtime Snapshot；无权限时不查询、不挂载对应资源，工具层再次拒绝调用，并保留明确诊断。
- 阶段 7：Run/Event/SSE 游标、审批恢复、审批中断取消、显式审批决定校验、停机即时收敛、重启收敛、对话 Trace、跨实例线程级 Run 抢占、失联 Run 回收后队列推进、运行中会话删除保护、checkpoint/事件/反馈/附件完整清理、默认工具快照挂载状态和运行任务注册并发安全均已验证；全量回归通过。
- 阶段 8：Dockerfile、Compose、`docker-manage.sh`、迁移启动顺序和国内镜像配置已完成；未在本机执行 Docker，目标服务器 smoke/fault 演练待部署方执行。
- 阶段 9：业务/原始物料分层、OSSIE/OMD/代码事实源收敛、按 commit 代码索引、SQL AST 解析、公共知识物料只读空间、历史物料归档和 Qdrant 清理已完成；真实数据库迁移已推进至 `20260915_0025`，6,488 条原始物料归档、14,896 个派生 chunk 清理，迁移重复执行无修改；阶段9专项、DataAgent/工具契约、认证 Agent API、Ruff、编译、Shell、Alembic 和 diff 检查均通过。

- 本轮静态/回归结果（2026-09-15）：后端全量回归 205 项通过，包含 RAG 快照授权、MCP 成功/失败发现、父 Agent 显式挂载子 Agent 的真实启动、无 workspace 权限用户创建临时会话、Git 仓库刷新清理残留文件和代码仓库符号链接隔离、Docker 首次密钥初始化、历史消息时间戳恢复、同时间 Run 稳定排序、默认策略跳过诊断抑制、代码逻辑按需挂载和纯闲聊工具隔离等增量测试；前端 lint/build、Python 编译和 `git diff --check` 通过。已补齐 Agent 显式挂载私有知识库在 RAG 检索层的授权传递、代码仓库按运行时知识库快照授权、新建 Agent 与编辑 Agent 的统一资源选择器（搜索、分类、全选/清空、固定内置项），以及父 Agent 显式挂载子 Agent 绕过角色重复分配的运行时授权。正式 DataAgent/通用 Agent Runtime 装配也已验证；同时修复了 DataAgent 固定 `metric_lookup` 未进入统一工具描述器导致的装配失败、临时 Workdir 在 macOS `/tmp` 符号链接下的写入路径问题、Skill 脚本未进入统一运行时和 `/outputs` 虚拟路径未映射的问题、MCP 连接错误中 URL/认证信息可能泄漏的问题、大规模 RAG 索引恢复阻塞 HTTP 启动的问题（改为服务就绪后后台恢复并提交），以及模型供应商/评估请求在未配置专用代理时继承宿主代理的问题（统一 `trust_env=False`）。已配置 MCP 的本机探针因 Docker/K8s 内部 DNS 不可解析未完成真实握手，代码已输出脱敏后的嵌套连接原因，仍需在容器网络中完成部署验收；对话文本附件已纳入统一 Runtime Context 并由 `read_attachment` 能力包读取，附件解析/OCR/部门共享旁路已移除；附件确认、制品另存和线程清理均执行受控路径校验；定时任务资源选择、线程级运行抢占、审批中断取消、显式审批决定校验、停机即时收敛和取消终态竞态、角色模块权限运行时闸门已纳入回归。
